function mouseoverPass(obj) {
    var obj = document.getElementById('password-id');
    obj.type = "text";
}

function mouseoutPass(obj) {
    var obj = document.getElementById('password-id');
    obj.type = "password";
}